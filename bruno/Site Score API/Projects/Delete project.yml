info:
  name: Delete project
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/projects/{{projectId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
