info:
  name: Get project by ID
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/projects/{{projectId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
