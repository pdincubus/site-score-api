info:
  name: Get project by ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/projects/{{projectId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
