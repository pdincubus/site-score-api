info:
  name: Get projects
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/projects"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
