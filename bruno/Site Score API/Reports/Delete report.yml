info:
  name: Delete report
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/reports/{{reportId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
