info:
  name: Update report by ID
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/reports/{{reportId}}"
  body:
    type: json
    data: |-
      {
        "title": "Updated report title",
        "performanceScore": 95
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
