info:
  name: Get reports for a project
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/projects/{{projectId}}/reports"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
